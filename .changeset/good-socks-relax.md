---
"@webmcp-bridge/local-mcp": patch
---

Improve `local-mcp` session recovery and process lifecycle management by exposing the `uxc/can_reap` contract, cleaning up managed Chromium processes more aggressively, and recovering Google attach sessions from stale persisted CDP metadata.
