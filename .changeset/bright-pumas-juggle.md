---
"@webmcp-bridge/local-mcp": patch
---

Add MCP service-level recovery instructions so clients that only see `bridge.*` tools know to check session status, bootstrap sign-in when needed, and attach again to expose site tools.
